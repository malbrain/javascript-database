#include "jsdb.h"
#include "jsdb_db.h"

uint32_t makeKey (uint8_t *keyBuff, DbDoc *doc, DbIndex *idx) {
	uint32_t keyLen = 0;
	return keyLen;
}

void insertKey (DbMap *index, uint32_t set, uint8_t *keyBuff, uint32_t keyLen) {
}
